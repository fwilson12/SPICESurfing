- [CONV] For a MOS current mirror to operate correctly, the output transistor’s drain–source voltage must stay at least equal to its overdrive (Vds ≥ Vgs−Vth); choose the output load (e.g., resistor value and connection) so the mirror can pull the output node high/low enough to maintain this saturation condition across expected operating currents.

